name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 8.15.9

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm

      - run: pnpm install --no-frozen-lockfile

      # gate:correctness builds, then runs eslint + typecheck and fails only
      # on violations beyond the frozen `.sandcastle/gate-baseline.json`
      # allowlist (swap#77). Replaces the old soft-gated `pnpm lint
      # continue-on-error: true` (a false-PASS hole — lint errors could never
      # fail CI) and adds typecheck to CI for the first time (previously
      # absent entirely).
      - run: pnpm run gate:correctness

      # Root `pnpm test` (not `pnpm -r test`) so the run picks up
      # vitest.config.ts's canonical include list, which covers both
      # packages/*/src/**/*.test.ts and .sandcastle/scripts/**/*.test.ts —
      # `pnpm -r test` would resolve packages/swap's own vitest.config.ts
      # instead and silently skip the .sandcastle gate-lib tests.
      - run: pnpm test

  devbox-validate:
    name: Devbox Environment Validation
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install devbox
        uses: jetify-com/devbox-install-action@v0.15.0
        with:
          enable-cache: true

      - name: Assert tool versions
        run: |
          cat > "${RUNNER_TEMP:-/tmp}/assert-versions.sh" <<'ASSERT'
          set -euo pipefail
          node --version | grep -E '^v22\.'
          pnpm --version | grep -E '^8\.15\.'
          forge --version
          cast --version
          anvil --version
          ASSERT
          devbox run -- bash "${RUNNER_TEMP:-/tmp}/assert-versions.sh"
          echo "✅ All devbox tools resolved at expected versions"

      - name: Smoke build
        run: devbox run -- bash -c "pnpm install --no-frozen-lockfile && pnpm build"

      # Devbox pins foundry, so `anvil` is on PATH here — this is where the
      # anvil-gated integration suite (incl. the swap#50 rolling-settlement
      # e2e, which spawns its own two anvil chains) actually RUNS instead of
      # runtime-skipping like it does on the plain build job.
      - name: Integration tests (anvil-gated)
        run: devbox run -- bash -c "pnpm --filter @toon-protocol/swap test:integration"

      # swap#104: the Docker cross-chain E2E suites' EVM leg is
      # self-contained (tests/e2e/global-setup.ts boots its own Anvil +
      # relay + peer1 in-process, needing only `anvil` on PATH — devbox
      # provides it here same as the integration step above). Solana/Mina
      # stay skip-only everywhere (see tests/e2e/README.md); the EVM core
      # failing to boot under CI is a real regression and fails this step
      # instead of masking as a pass-via-skip.
      - name: Docker cross-chain E2E (EVM leg, anvil-gated)
        run: devbox run -- bash -c "pnpm --filter @toon-protocol/swap test:e2e:docker"

  swap-image-build:
    name: swap runtime image build (issue #124)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Build-only (no push, no registry login) — validates
      # deploy/swap/Dockerfile on every PR/push, ahead of and independently
      # from publish-swap-image.yml's build-AND-push run on main.
      - name: Build swap runtime image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: deploy/swap/Dockerfile
          push: false
          tags: swap:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64

      # Same refusal-to-start check publish-swap-image.yml runs post-publish —
      # here it catches a Dockerfile/CLI regression before it ever reaches
      # main, instead of only after a push.
      - name: Verify image refuses to start without a mounted config
        run: |
          set +e
          OUTPUT=$(docker run --rm swap:ci 2>&1)
          STATUS=$?
          set -e
          echo "----- container output -----"
          echo "$OUTPUT"
          echo "----- exit code: $STATUS -----"
          if [ "$STATUS" -eq 0 ]; then
            echo "::error::swap image started with no config mounted — expected a refusal"
            exit 1
          fi
          echo "✓ Refused to start without a config, as expected"

  ci-ok:
    name: CI OK
    runs-on: ubuntu-latest
    needs: [build, devbox-validate, swap-image-build]
    if: always()
    steps:
      # Fleet-wide aggregate required check (toon-meta#279): this job is the
      # single required status for the repo. It must fail unless every gating
      # job actually SUCCEEDED — a skipped or cancelled gating job must never
      # read as a pass. `build`, `devbox-validate` and `swap-image-build` are
      # expected on every run; there are no legitimately-skippable jobs in
      # this workflow.
      - name: Check all gating jobs succeeded
        run: |
          echo "build: ${{ needs.build.result }}"
          echo "devbox-validate: ${{ needs.devbox-validate.result }}"
          echo "swap-image-build: ${{ needs.swap-image-build.result }}"
          if [ "${{ needs.build.result }}" != "success" ] || [ "${{ needs.devbox-validate.result }}" != "success" ] || [ "${{ needs.swap-image-build.result }}" != "success" ]; then
            echo "One or more gating jobs did not succeed (failure, cancelled, and skipped all fail)."
            exit 1
          fi
          echo "All gating jobs succeeded."
